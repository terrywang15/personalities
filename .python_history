exit()
import tensorflow_probability as tfp
a = [1,2,3]
b = [1,2,3]
dist = tfp.distributions.beta(a, b)
dist = tfp.python.distributions.beta(a, b)
import tensorflow_probability as tfp
tfd = tfp.distributions
dist = tfd.beta(a, b)
dist = tfd.Beta(a, b)
dist.sample([4, 5])
dist.sample([1])
dist.sample()
from random import sample
sample(range(10), 4)
int(100 *0.466)
set(range(100))
exit()
